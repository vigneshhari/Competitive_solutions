#include<iostream.h>
#include<conio.h>
#include<math.h>

void main()
{
clrscr();
int t,num,last_roll,n;
cin>>t;
for(int i=0;i<t;i++)
 {
 cin>>num;
 last_roll = pow(2,floor(log(num)/log(2)+1));
 n = (num - (last_roll - 1)) * 2 - 1;
 cout<<"BL:"<<floor(log((float)num)/log(2.0))<<"\n";
 }
getch();
}